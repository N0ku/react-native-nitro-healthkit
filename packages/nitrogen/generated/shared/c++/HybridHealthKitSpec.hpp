///
/// HybridHealthKitSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QuantityDataPoint` to properly resolve imports.
namespace margelo::nitro::healthkit { struct QuantityDataPoint; }
// Forward declaration of `CategoryDataPoint` to properly resolve imports.
namespace margelo::nitro::healthkit { struct CategoryDataPoint; }
// Forward declaration of `HealthData` to properly resolve imports.
namespace margelo::nitro::healthkit { struct HealthData; }
// Forward declaration of `WorkoutDataPoint` to properly resolve imports.
namespace margelo::nitro::healthkit { struct WorkoutDataPoint; }
// Forward declaration of `BackgroundSyncConfig` to properly resolve imports.
namespace margelo::nitro::healthkit { struct BackgroundSyncConfig; }

#include <NitroModules/Promise.hpp>
#include "QuantityDataPoint.hpp"
#include <vector>
#include <string>
#include <chrono>
#include <optional>
#include "CategoryDataPoint.hpp"
#include "HealthData.hpp"
#include "WorkoutDataPoint.hpp"
#include <unordered_map>
#include <functional>
#include "BackgroundSyncConfig.hpp"

namespace margelo::nitro::healthkit {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `HealthKit`
   * Inherit this class to create instances of `HybridHealthKitSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridHealthKit: public HybridHealthKitSpec {
   * public:
   *   HybridHealthKit(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridHealthKitSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridHealthKitSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridHealthKitSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> requestAuthorization() = 0;
      virtual std::shared_ptr<Promise<std::vector<QuantityDataPoint>>> getQuantityData(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::string>& aggregationType, std::optional<bool> useCache, std::optional<double> cacheTTL) = 0;
      virtual std::shared_ptr<Promise<double>> getAggregatedQuantity(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::string& aggregationType, std::optional<bool> useCache, std::optional<double> cacheTTL) = 0;
      virtual std::shared_ptr<Promise<std::vector<CategoryDataPoint>>> getCategoryData(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, std::optional<bool> useCache, std::optional<double> cacheTTL) = 0;
      virtual std::shared_ptr<Promise<HealthData>> getHealthDataForTimeRange(const std::string& timeRange, std::optional<std::chrono::system_clock::time_point> customStartDate, std::optional<std::chrono::system_clock::time_point> customEndDate, std::optional<bool> useCache, std::optional<double> cacheTTL) = 0;
      virtual std::shared_ptr<Promise<void>> clearCache() = 0;
      virtual std::shared_ptr<Promise<bool>> isHealthKitAvailable() = 0;
      virtual std::shared_ptr<Promise<std::string>> checkAuthorizationStatus(const std::string& type) = 0;
      virtual std::shared_ptr<Promise<std::vector<WorkoutDataPoint>>> getWorkouts(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, std::optional<bool> useCache, std::optional<double> cacheTTL) = 0;
      virtual std::shared_ptr<Promise<double>> getSteps(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) = 0;
      virtual std::shared_ptr<Promise<double>> getHeartRate(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) = 0;
      virtual std::shared_ptr<Promise<HealthData>> getHealthData(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) = 0;
      virtual std::shared_ptr<Promise<void>> seedTestHealthData() = 0;
      virtual std::shared_ptr<Promise<bool>> writeQuantityData(const std::string& type, double value, const std::string& unit, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::unordered_map<std::string, std::string>>& metadata) = 0;
      virtual std::shared_ptr<Promise<bool>> writeCategoryData(const std::string& type, double value, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::unordered_map<std::string, std::string>>& metadata) = 0;
      virtual std::shared_ptr<Promise<std::string>> observeQuantityChanges(const std::string& type, const std::function<void(const std::string& /* token */)>& callback) = 0;
      virtual std::shared_ptr<Promise<std::string>> observeCategoryChanges(const std::string& type, const std::function<void(const std::string& /* token */)>& callback) = 0;
      virtual std::shared_ptr<Promise<void>> removeObserver(const std::string& subscriptionId) = 0;
      virtual std::shared_ptr<Promise<void>> removeAllObservers() = 0;
      virtual std::shared_ptr<Promise<void>> registerBackgroundSync(const BackgroundSyncConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> unregisterBackgroundSync() = 0;
      virtual std::shared_ptr<Promise<bool>> isBackgroundSyncRegistered() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "HealthKit";
  };

} // namespace margelo::nitro::healthkit
