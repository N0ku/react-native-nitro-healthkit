///
/// BackgroundSyncConfig.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `BackgroundSyncConfig`, backed by a C++ struct.
 */
public typealias BackgroundSyncConfig = margelo.nitro.healthkit.BackgroundSyncConfig

public extension BackgroundSyncConfig {
  private typealias bridge = margelo.nitro.healthkit.bridge.swift

  /**
   * Create a new instance of `BackgroundSyncConfig`.
   */
  init(apiBaseUrl: String, jwtToken: String, intervalMinutes: Double, types: [String], syncPath: String) {
    self.init(std.string(apiBaseUrl), std.string(jwtToken), intervalMinutes, { () -> bridge.std__vector_std__string_ in
      var __vector = bridge.create_std__vector_std__string_(types.count)
      for __item in types {
        __vector.push_back(std.string(__item))
      }
      return __vector
    }(), std.string(syncPath))
  }

  var apiBaseUrl: String {
    @inline(__always)
    get {
      return String(self.__apiBaseUrl)
    }
    @inline(__always)
    set {
      self.__apiBaseUrl = std.string(newValue)
    }
  }
  
  var jwtToken: String {
    @inline(__always)
    get {
      return String(self.__jwtToken)
    }
    @inline(__always)
    set {
      self.__jwtToken = std.string(newValue)
    }
  }
  
  var intervalMinutes: Double {
    @inline(__always)
    get {
      return self.__intervalMinutes
    }
    @inline(__always)
    set {
      self.__intervalMinutes = newValue
    }
  }
  
  var types: [String] {
    @inline(__always)
    get {
      return self.__types.map({ __item in String(__item) })
    }
    @inline(__always)
    set {
      self.__types = { () -> bridge.std__vector_std__string_ in
        var __vector = bridge.create_std__vector_std__string_(newValue.count)
        for __item in newValue {
          __vector.push_back(std.string(__item))
        }
        return __vector
      }()
    }
  }
  
  var syncPath: String {
    @inline(__always)
    get {
      return String(self.__syncPath)
    }
    @inline(__always)
    set {
      self.__syncPath = std.string(newValue)
    }
  }
}
