///
/// JCategoryDataPoint.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "CategoryDataPoint.hpp"

#include <NitroModules/JInstant.hpp>
#include <chrono>
#include <optional>
#include <string>
#include <unordered_map>

namespace margelo::nitro::healthkit {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "CategoryDataPoint" and the the Kotlin data class "CategoryDataPoint".
   */
  struct JCategoryDataPoint final: public jni::JavaClass<JCategoryDataPoint> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/healthkit/CategoryDataPoint;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct CategoryDataPoint by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    CategoryDataPoint toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldValue = clazz->getField<double>("value");
      double value = this->getFieldValue(fieldValue);
      static const auto fieldStartDate = clazz->getField<JInstant>("startDate");
      jni::local_ref<JInstant> startDate = this->getFieldValue(fieldStartDate);
      static const auto fieldEndDate = clazz->getField<JInstant>("endDate");
      jni::local_ref<JInstant> endDate = this->getFieldValue(fieldEndDate);
      static const auto fieldMetadata = clazz->getField<jni::JMap<jni::JString, jni::JString>>("metadata");
      jni::local_ref<jni::JMap<jni::JString, jni::JString>> metadata = this->getFieldValue(fieldMetadata);
      return CategoryDataPoint(
        value,
        startDate->toChrono(),
        endDate->toChrono(),
        metadata != nullptr ? std::make_optional([&]() {
          std::unordered_map<std::string, std::string> __map;
          __map.reserve(metadata->size());
          for (const auto& __entry : *metadata) {
            __map.emplace(__entry.first->toStdString(), __entry.second->toStdString());
          }
          return __map;
        }()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JCategoryDataPoint::javaobject> fromCpp(const CategoryDataPoint& value) {
      using JSignature = JCategoryDataPoint(double, jni::alias_ref<JInstant>, jni::alias_ref<JInstant>, jni::alias_ref<jni::JMap<jni::JString, jni::JString>>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        value.value,
        JInstant::fromChrono(value.startDate),
        JInstant::fromChrono(value.endDate),
        value.metadata.has_value() ? [&]() -> jni::local_ref<jni::JMap<jni::JString, jni::JString>> {
          auto __map = jni::JHashMap<jni::JString, jni::JString>::create(value.metadata.value().size());
          for (const auto& __entry : value.metadata.value()) {
            __map->put(jni::make_jstring(__entry.first), jni::make_jstring(__entry.second));
          }
          return __map;
        }() : nullptr
      );
    }
  };

} // namespace margelo::nitro::healthkit
