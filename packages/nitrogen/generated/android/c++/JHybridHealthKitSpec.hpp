///
/// HybridHealthKitSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include <NitroModules/JHybridObject.hpp>
#include <fbjni/fbjni.h>
#include "HybridHealthKitSpec.hpp"




namespace margelo::nitro::healthkit {

  using namespace facebook;

  class JHybridHealthKitSpec: public jni::HybridClass<JHybridHealthKitSpec, JHybridObject>,
                              public virtual HybridHealthKitSpec {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/packages/HybridHealthKitSpec;";
    static jni::local_ref<jhybriddata> initHybrid(jni::alias_ref<jhybridobject> jThis);
    static void registerNatives();

  protected:
    // C++ constructor (called from Java via `initHybrid()`)
    explicit JHybridHealthKitSpec(jni::alias_ref<jhybridobject> jThis) :
      HybridObject(HybridHealthKitSpec::TAG),
      HybridBase(jThis),
      _javaPart(jni::make_global(jThis)) {}

  public:
    ~JHybridHealthKitSpec() override {
      // Hermes GC can destroy JS objects on a non-JNI Thread.
      jni::ThreadScope::WithClassLoader([&] { _javaPart.reset(); });
    }

  public:
    size_t getExternalMemorySize() noexcept override;
    void dispose() noexcept override;

  public:
    inline const jni::global_ref<JHybridHealthKitSpec::javaobject>& getJavaPart() const noexcept {
      return _javaPart;
    }

  public:
    // Properties
    

  public:
    // Methods
    std::shared_ptr<Promise<bool>> requestAuthorization() override;
    std::shared_ptr<Promise<std::vector<QuantityDataPoint>>> getQuantityData(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::string>& aggregationType, std::optional<bool> useCache, std::optional<double> cacheTTL) override;
    std::shared_ptr<Promise<double>> getAggregatedQuantity(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::string& aggregationType, std::optional<bool> useCache, std::optional<double> cacheTTL) override;
    std::shared_ptr<Promise<std::vector<CategoryDataPoint>>> getCategoryData(const std::string& type, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, std::optional<bool> useCache, std::optional<double> cacheTTL) override;
    std::shared_ptr<Promise<HealthData>> getHealthDataForTimeRange(const std::string& timeRange, std::optional<std::chrono::system_clock::time_point> customStartDate, std::optional<std::chrono::system_clock::time_point> customEndDate, std::optional<bool> useCache, std::optional<double> cacheTTL) override;
    std::shared_ptr<Promise<void>> clearCache() override;
    std::shared_ptr<Promise<bool>> isHealthKitAvailable() override;
    std::shared_ptr<Promise<std::string>> checkAuthorizationStatus(const std::string& type) override;
    std::shared_ptr<Promise<std::vector<WorkoutDataPoint>>> getWorkouts(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, std::optional<bool> useCache, std::optional<double> cacheTTL) override;
    std::shared_ptr<Promise<double>> getSteps(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) override;
    std::shared_ptr<Promise<double>> getHeartRate(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) override;
    std::shared_ptr<Promise<HealthData>> getHealthData(std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate) override;
    std::shared_ptr<Promise<void>> seedTestHealthData() override;
    std::shared_ptr<Promise<bool>> writeQuantityData(const std::string& type, double value, const std::string& unit, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::unordered_map<std::string, std::string>>& metadata) override;
    std::shared_ptr<Promise<bool>> writeCategoryData(const std::string& type, double value, std::chrono::system_clock::time_point startDate, std::chrono::system_clock::time_point endDate, const std::optional<std::unordered_map<std::string, std::string>>& metadata) override;
    std::shared_ptr<Promise<std::string>> observeQuantityChanges(const std::string& type, const std::function<void(const std::string& /* token */)>& callback) override;
    std::shared_ptr<Promise<std::string>> observeCategoryChanges(const std::string& type, const std::function<void(const std::string& /* token */)>& callback) override;
    std::shared_ptr<Promise<void>> removeObserver(const std::string& subscriptionId) override;
    std::shared_ptr<Promise<void>> removeAllObservers() override;
    std::shared_ptr<Promise<void>> registerBackgroundSync(const BackgroundSyncConfig& config) override;
    std::shared_ptr<Promise<void>> unregisterBackgroundSync() override;
    std::shared_ptr<Promise<bool>> isBackgroundSyncRegistered() override;

  private:
    friend HybridBase;
    using HybridBase::HybridBase;
    jni::global_ref<JHybridHealthKitSpec::javaobject> _javaPart;
  };

} // namespace margelo::nitro::healthkit
